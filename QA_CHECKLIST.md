# QA — сквозная проверка Rental_bish (сайт + админка + ИИ)

Цель: убедиться, что вся связка работает вместе — заявка с сайта доходит до админки и превращается в бронь, ИИ-ассистент видит **реальные** данные склада и меняет дашборд, а роли соблюдаются на сервере.

Отмечайте: ✅ прошёл · ❌ упал (заведите баг) · ⚠️ прошёл с замечанием.

---

## 0. Окружение и доступ

| Что | Адрес |
|---|---|
| Сайт | `https://home-server-wsl.tail14ec1a.ts.net/` (или локально `http://home-server-wsl/`) |
| Админка | тот же домен, путь `/admin` |
| API (для проверки) | тот же домен, `/api/v1/...` |

**Учётки для теста** (заведите заранее в разделе «Сотрудники» под админом):
- `admin` / `admin` — роль **Администратор**
- `qa_staff` / (пароль) — роль **Сотрудник склада**

**Браузеры:** Chrome/Firefox (десктоп) + мобильный вид (DevTools → responsive, iPhone/Android).

> Перед каждой сессии открывайте консоль DevTools — **в норме ошибок быть не должно** (кроме `favicon.ico 404` — он безвреден).

---

## 1. Инфраструктура — единый домен (smoke)

| # | Шаг | Ожидаемо |
|---|---|---|
| 1.1 | Открыть `/` | Витрина Rental_bish, HTTP 200 |
| 1.2 | Открыть `/admin` | Редирект на `/admin/login`, форма входа |
| 1.3 | Открыть `/api/v1/openapi.json` | JSON схемы API, 200 |
| 1.4 | На витрине открыть карточку товара с фото | Фото грузится (путь `/media/...`, 200) |
| 1.5 | Проверить, что всё на **одном домене** | В Network все запросы идут на текущий origin, без `:8010`/`:5173`/`:5174` |

---

## 2. Сайт-витрина (глазами клиента)

| # | Шаг | Ожидаемо |
|---|---|---|
| 2.1 | Пролистать главную | Все секции: Hero, категории, преимущества, превью каталога, форма заявки, «Как арендовать», галерея, баннер, контакты (карта) |
| 2.2 | Меню «Как арендовать»/«Контакты» с других страниц | Уводит на главную и плавно скроллит к секции |
| 2.3 | Открыть **Каталог**, переключить чипы категорий | Список сужается по категории; секции сворачиваются/разворачиваются |
| 2.4 | Карточка товара: цена, бейдж «Новинка», «В наличии» | Цена «от N сом», бейдж у новых, наличие показано |
| 2.5 | Открыть модалку товара | Галерея фото, описание, характеристики, «В наличии: N шт.», Esc/фон/× закрывают |
| 2.6 | «+ в заявку» у товара **не в наличии** | Кнопка заблокирована |
| 2.7 | Добавить товар, крутить счётчик «− N +» | «+» упирается в остаток (нельзя больше наличия) |
| 2.8 | Корзина → указать Дату и Срок (сут.) | Итог = аренда × число суток, минимум 1 сутки |
| 2.9 | Отправить без имени/телефона | Ошибка «Укажите имя и телефон» |
| 2.10 | Отправить с именем и телефоном | Открывается WhatsApp с текстом; корзина очищается; «Заявка отправлена» |
| 2.11 | Обновить страницу с товарами в корзине (до отправки) | Корзина сохранилась (localStorage) |
| 2.12 | Мобильный вид | Без горизонтальной прокрутки, всё читаемо |

> После шага 2.10 **не закрывайте** — эта заявка нужна в блоке 4 (доходит до админки).

---

## 3. Вход в админку и роли

| # | Шаг | Ожидаемо |
|---|---|---|
| 3.1 | Войти под `admin` | Дашборд «Обзор»; в меню видны **Точки, Сотрудники, Аудит, Категории** |
| 3.2 | Выйти, войти под `qa_staff` | Дашборд открылся; админских разделов в меню **нет** |
| 3.3 | Под `qa_staff` открыть `/admin/points` вручную | Нет доступа / редирект (раздел админский) |
| 3.4 | Неверный пароль | Вход отклонён с ошибкой |

---

## 4. 🔗 Золотой сквозной сценарий (сайт → админка → бронь)

Главная проверка связки. Выполнять под `qa_staff` (кроме отмеченного).

| # | Шаг | Ожидаемо |
|---|---|---|
| 4.1 | Админка → **Заявки** | Заявка из шага 2.10 в списке, статус **Новая**, подсвечена |
| 4.2 | Проверить содержимое | Имя, телефон, позиции × кол-во, комментарий (дата/тип/гости) — как отправляли |
| 4.3 | «В бронь» → задать Точку, Дату начала, Дату возврата, Предоплату → «Оформить бронь» | Создана бронь; заявка → статус **В брони**, появилась ссылка «Бронь №N» |
| 4.4 | Открыть бронь → «Подтвердить» (если черновик) | Статус **Подтверждена**, остаток зарезервирован |
| 4.5 | «Выдать» (можно частично) | Позиции ушли «на руки»; статус **Выдана** |
| 4.6 | Проверить **Остатки** по этому товару | «Выдано» увеличилось, «Доступно» уменьшилось |
| 4.7 | «Принять возврат»: часть «Целых», часть «Бой» | Посчитано «Удержание за бой» по залоговой цене; при полном возврате — **Закрыта** |
| 4.8 | Открыть карточку клиента этой брони | Бронь в истории; «Долг»/«На руках» отражают операции |

---

## 5. Товары, фото, импорт

| # | Шаг | Ожидаемо |
|---|---|---|
| 5.1 | «Новый товар»: заполнить, **без фото**, «Показывать на сайте» ✓ | Создан; на сайте **не** показывается (нет фото) |
| 5.2 | Добавить фото (загрузка файла), назначить главным | Появилось; на сайте товар теперь виден |
| 5.3 | Создать **Комплект** с составом | Доступность считается по самому дефицитному компоненту |
| 5.4 | Excel-импорт: «Скачать шаблон» → заполнить → загрузить | Предпросмотр строк + список проблемных до сохранения; «Импортировать (N)» создаёт товары |
| 5.5 | Залоговая стоимость | Влияет на удержание за бой (сверить с шагом 4.7) |

---

## 6. Остатки (завоз/списание)

| # | Шаг | Ожидаемо |
|---|---|---|
| 6.1 | «Завоз»: товар, точка, кол-во | «Всего»/«Доступно» выросли |
| 6.2 | «Списание» причина **Бой** | Удержание = кол-во × залоговая цена; остаток уменьшился |
| 6.3 | «Списание» причина **Износ** | Удержание = 0 |
| 6.4 | Формула | Доступно = Всего − Бронь − Выдано (сверить визуально) |

---

## 7. Отчёты

| # | Шаг | Ожидаемо |
|---|---|---|
| 7.1 | «Остатки» + выгрузка Excel | Данные совпадают с разделом «Остатки»; файл скачивается |
| 7.2 | «На руках и просрочки», фильтр «Только просрочки» | Список бронёй с остатком на руках/просрочкой |
| 7.3 | «Движение за период» с датами | Плитки Завезено/Списано/Удержано/Выдач/Возвратов |
| 7.4 | «Активность сотрудников» под `qa_staff` | Раздела **нет** (только админ) |
| 7.5 | То же под `admin` | Раздел есть, по логинам считает операции |

---

## 8. 🤖 ИИ-ассистент — реальные данные (Фаза 2)

Крайне важный блок: ассистент должен читать **настоящие** данные, а не выдумывать.

| # | Шаг | Ожидаемо |
|---|---|---|
| 8.1 | Кнопка-искра внизу справа — на каждом экране | Открывается правая панель «ИИ-ассистент» |
| 8.2 | Спросить «**Что просрочено?**» | Отвечает **числом/списком**, совпадающим с блоком «Просроченные возвраты» и отчётом 7.2 |
| 8.3 | Спросить «**Что сейчас на руках?**» | Совпадает с плиткой «На руках» и карточками клиентов |
| 8.4 | Спросить «**Какой долг у клиента `<имя/телефон>`?**» | Совпадает с «Долг» в карточке клиента |
| 8.5 | Изменить данные (создать/закрыть бронь) и переспросить | Ответ ИИ **отражает изменение** (данные живые, не кэш выдумки) |
| 8.6 | Ответ с форматированием | Рендерится markdown (списки/жирный), а не сырой текст |

### 8а. ИИ меняет дашборд — под `admin`

| # | Шаг | Ожидаемо |
|---|---|---|
| 8а.1 | «**Добавь блок с выручкой по дням за 30 дней**» | На «Обзор» появляется новый виджет с бейджем **«от ИИ»** |
| 8а.2 | «**Какие блоки на дашборде?**» | Перечисляет блоки, включая добавленный |
| 8а.3 | «**Убери этот блок**» (или через «Настроить» → «Удалить») | Виджет исчезает с дашборда |
| 8а.4 | «Настроить» (только админ): скрыть/переставить встроенный блок | Встроенный **нельзя удалить**, только скрыть/переставить |

### 8б. 🔒 Роль соблюдается на СЕРВЕРЕ — под `qa_staff`

Ключевая security-проверка: сотрудник не должен менять дашборд, даже если попросит ИИ.

| # | Шаг | Ожидаемо |
|---|---|---|
| 8б.1 | Под `qa_staff` попросить «**Добавь блок …**» | Отказ: «Менять состав дашборда может только администратор» |
| 8б.2 | Обновить «Обзор» | Новый блок **НЕ добавился** (проверка на сервере, не «на словах» модели) |
| 8б.3 | Настойчиво просить/переформулировать несколько раз | Всё равно отказ, блок не появляется |

### 8в. История чатов (Фаза 1)

| # | Шаг | Ожидаемо |
|---|---|---|
| 8в.1 | Задать вопрос, **обновить страницу**, открыть панель | Переписка на месте (история на сервере) |
| 8в.2 | Второй вопрос в том же чате | Ответ учитывает контекст первого |
| 8в.3 | «+ Новый чат» | Пустой чат с чистого листа |
| 8в.4 | «Чаты · N» → удалить чат (корзина) | Чат и его сообщения исчезли |
| 8в.5 | Под другим сотрудником открыть список чатов | Чужих чатов **не видно** (изоляция по сотруднику) |
| 8в.6 | Задать вопрос при выключенном шлюзе (негативный, если воспроизводимо) | «ИИ-ассистент временно недоступен» (HTTP 503), переписка не портится |

---

## 9. Админские разделы (под `admin`)

| # | Шаг | Ожидаемо |
|---|---|---|
| 9.1 | **Точки**: создать, отключить/включить | Бейдж «активна/отключена» переключается; в бронях доступны только активные точки |
| 9.2 | **Сотрудники**: создать, задать роль, отключить | Логин не меняется после создания; отключённый не входит |
| 9.3 | **Сотрудники**: сменить дефолтный пароль `admin` | Пароль обновлён (проверить входом) |
| 9.4 | **Категории**: создать/переименовать | Отражается в товарах и на сайте |
| 9.5 | **Аудит**: выполнить операции и открыть журнал | Операции (create/confirm/issue/return/cancel/supply/write_off) с логином и объектом; фильтр по типу работает |

---

## 10. Кросс-проверки (везде)

| # | Шаг | Ожидаемо |
|---|---|---|
| 10.1 | Настройки → Тема «Тёмная» | Вся админка перекрашивается, ничего не разъезжается |
| 10.2 | Настройки → Масштаб 130% | Вёрстка не ломается, без горизонтальной прокрутки |
| 10.3 | Мобильный вид админки | Таблицы → карточки, меню доступно |
| 10.4 | Консоль DevTools во всех разделах | Нет ошибок (кроме favicon 404) |
| 10.5 | Прямой переход по `/admin/bookings/<id>` (deep link) | Открывается нужная бронь (роутинг под `/admin` работает) |

---

## Что считать блокером

- Заявка с сайта **не доходит** до «Заявок» или теряет позиции (блок 4).
- ИИ **выдумывает** числа вместо реальных (блок 8, шаги 8.2–8.5).
- Сотрудник **смог** добавить/удалить блок дашборда (блок 8б) — дыра в правах.
- Резерв/остатки считаются неверно (двойное списание, уход в минус) — блоки 4, 6.
- Чужие чаты/данные видны другому сотруднику (8в.5).

## Шаблон бага

```
[Раздел / шаг чек-листа]: например 8б.2
Роль: qa_staff / admin
Шаги: 1) ... 2) ...
Ожидалось: ...
Фактически: ...
Окружение: браузер, десктоп/мобайл, URL
Консоль/сеть: (ошибки, коды ответов)
Скриншот: ...
```
